<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no">
<title>Good Planner</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Italiana&family=Cormorant+Garamond:ital,wght@0,500;0,600;1,500&family=Poppins:wght@400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root{
    /* Default theme — purple & gold planner palette */
    --bg-1:#f4f4fa;
    --bg-2:#eeedf7;
    --bg-3:#e8e6f4;
    --surface:#ffffff;
    --surface-raised:#ffffff;
    --surface-inset:#f2f1f9;
    --surface-pressed:#e9e7f4;
    --border:rgba(45,27,94,0.10);
    --border-soft:rgba(45,27,94,0.06);
    --border-accent:rgba(75,47,138,0.45);
    --accent-peach:#f6c945;
    --accent-peach-deep:#e0ad1f;
    --accent-blue:#4b2f8a;
    --accent-blue-deep:#2d1b5e;
    --accent-blush:#e8dcff;
    --text-hi:#221638;
    --text-mid:#7b7191;
    --text-low:#aca5c0;
    --success:#4caf7d;
    --warning:#e0ad1f;
    --danger:#e0615a;
    --shadow-soft: 0 1px 2px rgba(45,27,94,0.05), 0 10px 22px rgba(45,27,94,0.08);
    --shadow-deep: 0 3px 8px rgba(45,27,94,0.10), 0 24px 48px rgba(45,27,94,0.20);
    --radius-s:10px;
    --radius-m:16px;
    --radius-l:22px;
    --radius-full:999px;
    --header-grad: linear-gradient(160deg, #4b2f8a 0%, #2d1b5e 100%);
    --nav-bg: #2d1b5e;
    --glass-bg: rgba(255,255,255,0.14);
    --glass-border: rgba(255,255,255,0.22);
    --glass-blur: blur(22px) saturate(160%);
    --glass-shadow: 0 10px 30px rgba(20,10,45,0.35);
    --nav-item-color: rgba(255,255,255,0.6);
    --nav-item-active-bg: linear-gradient(135deg, var(--accent-peach), var(--accent-peach-deep));
    --nav-item-active-color: #2d1b5e;
  }

  /* Classic (original) warm theme — toggled on from Settings */
  body.theme-old{
    --bg-1:#ecdccf;
    --bg-2:#e3cec4;
    --bg-3:#d9c3ba;
    --surface:#f8efe2;
    --surface-raised:#fdf8ef;
    --surface-inset:#f1e6d6;
    --surface-pressed:#ecdfcc;
    --border:rgba(122,90,66,0.12);
    --border-soft:rgba(122,90,66,0.07);
    --border-accent:rgba(214,148,109,0.55);
    --accent-peach:#e2a077;
    --accent-peach-deep:#d3835a;
    --accent-blue:#82a9c9;
    --accent-blue-deep:#5f89ac;
    --accent-blush:#dcaba4;
    --text-hi:#4a3a30;
    --text-mid:#8a7566;
    --text-low:#ad998b;
    --success:#84ac86;
    --warning:#dba24f;
    --danger:#cf7b68;
    --shadow-soft: 0 1px 2px rgba(110,80,55,0.06), 0 10px 22px rgba(110,80,55,0.10);
    --shadow-deep: 0 3px 8px rgba(110,80,55,0.10), 0 24px 48px rgba(110,80,55,0.20);
    --header-grad: linear-gradient(180deg, rgba(236,220,207,0.96) 70%, rgba(236,220,207,0));
    --nav-bg: rgba(248,239,226,0.92);
    --glass-bg: rgba(253,248,239,0.45);
    --glass-border: rgba(122,90,66,0.18);
    --glass-blur: blur(22px) saturate(160%);
    --glass-shadow: 0 10px 30px rgba(90,60,35,0.18);
    --nav-item-color: var(--text-low);
    --nav-item-active-bg: linear-gradient(135deg, var(--accent-peach), var(--accent-blue));
    --nav-item-active-color: #fff;
  }

  *{box-sizing:border-box; -webkit-tap-highlight-color:transparent;}
  html,body{height:100%;}
  body{
    margin:0;
    font-family:'Poppins',sans-serif;
    background:
      radial-gradient(900px 480px at 10% -10%, var(--bg-1) 0%, transparent 55%),
      radial-gradient(800px 460px at 105% 15%, var(--bg-2) 0%, transparent 50%),
      linear-gradient(165deg, var(--bg-1) 0%, var(--bg-2) 55%, var(--bg-3) 100%);
    color:var(--text-hi);
    min-height:100%;
    overflow-x:hidden;
    transition: background .35s ease, color .35s ease;
  }
  body, body *{ transition: background-color .3s ease, border-color .3s ease, color .3s ease, box-shadow .3s ease; }

  /* ---------- Theme toggle switch ---------- */
  .theme-row{
    display:flex; align-items:center; justify-content:space-between; margin-top:12px;
  }
  .theme-row-text{min-width:0; padding-right:12px;}
  .theme-row-title{font-size:13.5px; font-weight:600; color:var(--text-hi);}
  .theme-row-sub{font-size:11.5px; color:var(--text-low); margin-top:2px;}
  .switch{
    position:relative; width:48px; height:28px; flex-shrink:0; cursor:pointer;
    background:var(--surface-inset); border:1px solid var(--border); border-radius:var(--radius-full);
  }
  .switch .knob{
    position:absolute; top:2px; left:2px; width:22px; height:22px; border-radius:50%;
    background:var(--surface-raised); box-shadow:var(--shadow-soft);
    transition:transform .25s cubic-bezier(.2,.9,.25,1), background .25s ease;
  }
  .switch.on{background:linear-gradient(135deg, var(--accent-peach), var(--accent-blue)); border-color:var(--border-accent);}
  .switch.on .knob{transform:translateX(20px);}
  ::selection{background:rgba(226,160,119,0.35);}
  button{font-family:inherit;}
  input,textarea{font-family:inherit;}
  .wrap-text{overflow-wrap:anywhere; word-break:break-word;}

  /* ---------- Splash ---------- */
  #splash{
    position:fixed; inset:0; z-index:999;
    display:flex; flex-direction:column; align-items:center; justify-content:center;
    background:linear-gradient(165deg, var(--bg-1) 0%, var(--bg-2) 55%, var(--bg-3) 100%);
    transition:opacity .6s ease, visibility .6s ease;
  }
  #splash.hide{opacity:0; visibility:hidden; pointer-events:none;}
  .logo-mark{
    width:88px; height:88px;
    border-radius:50%;
    position:relative;
    box-shadow: var(--shadow-deep);
    animation: rise .9s cubic-bezier(.2,.9,.25,1) both;
    background:var(--surface-raised);
  }
  .logo-mark svg{width:88px;height:88px; display:block;}
  .logo-ring{
    position:absolute; inset:-12px; border-radius:50%;
    border:1px solid var(--border-accent);
    opacity:0; animation: pulse 1.8s ease-out .3s infinite;
  }
  @keyframes rise{
    from{opacity:0; transform:translateY(14px) scale(.9);}
    to{opacity:1; transform:translateY(0) scale(1);}
  }
  @keyframes pulse{
    0%{opacity:.5; transform:scale(1);}
    100%{opacity:0; transform:scale(1.4);}
  }
  .splash-word{
    margin-top:24px;
    font-family:'Italiana',serif;
    font-weight:400;
    font-size:30px; letter-spacing:1.5px;
    color:var(--text-hi);
    opacity:0; animation: fadeUp .7s ease .25s both;
  }
  .splash-sub{
    margin-top:8px;
    font-family:'Cormorant Garamond',serif; font-style:italic;
    font-size:14px; letter-spacing:1px;
    color:var(--text-mid);
    opacity:0; animation: fadeUp .7s ease .4s both;
  }
  @keyframes fadeUp{
    from{opacity:0; transform:translateY(6px);}
    to{opacity:1; transform:translateY(0);}
  }
  .splash-bar{
    width:120px; height:2px; border-radius:2px;
    background:var(--surface-inset);
    margin-top:30px; overflow:hidden;
    opacity:0; animation: fadeUp .5s ease .5s both;
  }
  .splash-bar i{
    display:block; height:100%; width:40%; border-radius:2px;
    background:linear-gradient(90deg, var(--accent-peach), var(--accent-blue));
    animation: load 1.1s ease-in-out infinite;
  }
  @keyframes load{
    0%{transform:translateX(-100%);}
    100%{transform:translateX(340%);}
  }

  /* ---------- App shell ---------- */
  #app{
    display:none;
    max-width:480px; margin:0 auto;
    min-height:100vh;
    position:relative;
    padding-bottom:100px;
    overflow-x:hidden;
  }
  #app.show{display:block; animation: fadeIn .5s ease both;}
  @keyframes fadeIn{from{opacity:0;} to{opacity:1;}}

  header.top{
    position:sticky; top:14px; z-index:20;
    margin:14px 16px 10px;
    padding:14px 16px;
    background:var(--glass-bg);
    -webkit-backdrop-filter:var(--glass-blur);
    backdrop-filter:var(--glass-blur);
    border:1px solid var(--glass-border);
    border-radius:var(--radius-full);
    box-shadow:var(--glass-shadow);
  }
  .top-row{display:flex; align-items:center; justify-content:space-between;}
  .brand{display:flex; align-items:center; gap:11px;}
  .brand-mark{
    width:34px;height:34px;border-radius:50%;
    box-shadow:var(--shadow-soft);
    display:flex; align-items:center; justify-content:center;
    flex-shrink:0;
  }
  .brand-mark svg{width:34px;height:34px;}
  .brand-name{font-family:'Italiana',serif; font-weight:400; font-size:20px; letter-spacing:.8px; color:var(--text-hi);}
  .icon-btn{
    width:38px; height:38px; border-radius:var(--radius-full);
    background:rgba(255,255,255,0.35); border:1px solid var(--glass-border);
    display:flex; align-items:center; justify-content:center;
    color:var(--text-mid); cursor:pointer;
    transition:background .15s ease, transform .15s ease, color .15s ease;
  }
  .icon-btn:active{transform:scale(.94);}
  .icon-btn svg{width:18px;height:18px;}

  main{padding:4px 20px 20px; max-width:100%;}

  /* ---------- Date nav ---------- */
  .date-nav{
    display:flex; align-items:center; justify-content:space-between;
    margin-top:10px; margin-bottom:18px;
  }
  .date-label{cursor:pointer; min-width:0;}
  .date-eyebrow{font-size:11px; letter-spacing:2px; text-transform:uppercase; color:var(--accent-peach-deep); margin-bottom:3px; font-weight:600;}
  .date-title{font-family:'Italiana',serif; font-size:23px; font-weight:400; letter-spacing:.3px;}
  .date-arrows{display:flex; gap:8px;}
  .arrow-btn{
    width:34px;height:34px;border-radius:var(--radius-full);
    background:var(--surface); border:1px solid var(--border);
    display:flex;align-items:center;justify-content:center;color:var(--text-mid);cursor:pointer;
    box-shadow:var(--shadow-soft); flex-shrink:0;
  }
  .arrow-btn:active{background:var(--surface-pressed); transform:scale(.94);}
  .today-chip{
    font-size:11px; padding:5px 11px; border-radius:var(--radius-full);
    background:var(--surface); border:1px solid var(--border);
    color:var(--text-mid); cursor:pointer; letter-spacing:.4px;
  }
  .today-chip.active{color:var(--accent-peach-deep); border-color:var(--border-accent); background:rgba(226,160,119,0.1);}

  /* ---------- Progress ring summary ---------- */
  .day-summary{
    display:flex; align-items:center; gap:14px;
    background:var(--header-grad);
    border-radius:var(--radius-l); padding:18px 20px;
    box-shadow:var(--shadow-deep);
    margin-bottom:22px;
  }
  body.theme-old .day-summary{ box-shadow:var(--shadow-soft); }
  .ring-wrap{position:relative; width:52px; height:52px; flex-shrink:0;}
  .ring-wrap svg{transform:rotate(-90deg);}
  .ring-pct{position:absolute; inset:0; display:flex; align-items:center; justify-content:center; font-size:12px; font-weight:700; font-family:'DM Mono',monospace; color:#fff;}
  body.theme-old .ring-pct{ color:var(--accent-peach-deep); }
  .summary-text{min-width:0; flex:1;}
  .summary-text .st-title{font-size:14.5px; font-weight:600; color:#fff; margin-bottom:2px;}
  .summary-text .st-sub{font-size:12px; color:rgba(255,255,255,0.75);}
  body.theme-old .summary-text .st-title{ color:var(--text-hi); }
  body.theme-old .summary-text .st-sub{ color:var(--text-low); }

  /* ---------- Timeline / Agenda ---------- */
  .section-label{
    font-size:11px; letter-spacing:2px; text-transform:uppercase; color:var(--text-low); font-weight:600;
    margin:22px 0 12px 0; display:flex; align-items:center; justify-content:space-between;
  }
  .timeline{position:relative; padding-left:6px; max-width:100%;}
  .timeline::before{
    content:''; position:absolute; left:29px; top:6px; bottom:6px; width:1px;
    background:linear-gradient(180deg, var(--border-accent), var(--border) 70%);
  }
  .agenda-item{
    position:relative; display:flex; gap:14px; margin-bottom:10px;
    animation: itemIn .35s ease both;
    max-width:100%;
  }
  @keyframes itemIn{from{opacity:0; transform:translateY(6px);} to{opacity:1; transform:translateY(0);}}
  .time-col{width:58px; flex-shrink:0; text-align:right; padding-top:14px; position:relative;}
  .time-text{font-family:'DM Mono',monospace; font-size:11px; color:var(--text-low);}
  .node{
    position:absolute; left:53px; top:17px; width:9px; height:9px; border-radius:50%;
    background:var(--surface); border:2px solid var(--accent-blue); z-index:2;
  }
  .agenda-item.done .node{background:var(--success); border-color:var(--success);}
  .agenda-item.reminder .node{background:var(--accent-peach); border-color:var(--accent-peach); border-radius:3px; width:8px; height:8px; transform:rotate(45deg);}

  .item-card{
    flex:1; min-width:0; background:var(--surface);
    border-left:4px solid var(--accent-peach);
    border-radius:var(--radius-m); padding:13px 14px;
    display:flex; align-items:flex-start; gap:11px;
    box-shadow:var(--shadow-soft);
    transition:transform .12s ease, border-color .15s ease;
    cursor:pointer;
  }
  .agenda-item.reminder .item-card{ border-left-color:var(--accent-blue); }
  .agenda-item.done .item-card{ border-left-color:var(--success); }
  .item-card:active{transform:scale(.985);}
  .agenda-item.done .item-card{opacity:.6;}
  .check{
    width:22px;height:22px;border-radius:7px;flex-shrink:0;cursor:pointer; margin-top:1px;
    border:1.5px solid var(--text-low); background:var(--surface-raised);
    display:flex;align-items:center;justify-content:center;
    transition:background .15s ease, border-color .15s ease;
  }
  .check.checked{background:var(--success); border-color:var(--success);}
  .check svg{width:12px;height:12px; opacity:0;}
  .check.checked svg{opacity:1;}
  .item-body{flex:1; min-width:0; overflow:hidden;}
  .item-title{font-size:14.5px; font-weight:600; color:var(--text-hi); line-height:1.35;}
  .agenda-item.done .item-title{text-decoration:line-through; text-decoration-color:var(--text-low);}
  .item-desc{font-size:12px; color:var(--text-mid); line-height:1.5; margin-top:4px;}
  .item-tag{font-size:10.5px; color:var(--accent-peach-deep); display:flex; align-items:center; gap:4px; margin-top:5px; font-weight:600;}
  .item-tag svg{width:11px;height:11px; flex-shrink:0;}
  .del-btn{
    width:26px;height:26px;border-radius:var(--radius-full); flex-shrink:0;
    display:flex;align-items:center;justify-content:center; color:var(--text-low); cursor:pointer;
    opacity:.6;
  }
  .del-btn:active{opacity:1; color:var(--danger);}
  .del-btn svg{width:14px;height:14px;}

  .empty-state{
    text-align:center; padding:40px 20px;
    background:var(--surface-inset); border:1px dashed var(--border);
    border-radius:var(--radius-l); color:var(--text-low);
  }
  .empty-state svg{width:34px;height:34px;margin-bottom:10px; opacity:.6; color:var(--accent-blue-deep);}
  .empty-state .es-title{font-size:14px; color:var(--text-mid); font-weight:600; margin-bottom:4px;}
  .empty-state .es-sub{font-size:12.5px;}

  /* ---------- FAB ---------- */
  .fab{
    position:fixed; right:calc(50% - 240px + 20px); bottom:96px; z-index:30;
    width:58px; height:58px; border-radius:var(--radius-full);
    background:linear-gradient(135deg, var(--accent-peach), var(--accent-peach-deep));
    display:flex; align-items:center; justify-content:center;
    box-shadow:0 10px 22px rgba(224,173,31,0.45), 0 0 0 6px var(--bg-1);
    cursor:pointer; color:var(--accent-blue-deep); border:none;
  }
  body.theme-old .fab{ background:linear-gradient(135deg, var(--accent-peach), var(--accent-blue)); color:#3a2a20; box-shadow:0 12px 26px rgba(180,130,95,0.35), var(--shadow-soft); }
  .fab:active{transform:scale(.93);}
  .fab svg{width:24px;height:24px;}
  @media (max-width:520px){ .fab{right:20px;} }

  /* ---------- Bottom nav ---------- */
  nav.bottom{
    position:fixed; left:0; right:0; bottom:0; z-index:25;
    display:flex; justify-content:center;
    padding-bottom:calc(18px + env(safe-area-inset-bottom));
    pointer-events:none;
  }
  .nav-inner{
    pointer-events:auto;
    width:auto;
    display:flex; justify-content:center; align-items:center; gap:2px;
    padding:8px;
    background:var(--glass-bg);
    -webkit-backdrop-filter:var(--glass-blur);
    backdrop-filter:var(--glass-blur);
    border:1px solid var(--glass-border);
    border-radius:var(--radius-full);
    box-shadow:var(--glass-shadow);
  }
  .nav-btn{
    display:flex; flex-direction:row; align-items:center; gap:7px;
    color:var(--nav-item-color); cursor:pointer; padding:11px 16px; border-radius:var(--radius-full);
    transition:color .2s ease, background .2s ease, padding .2s ease;
  }
  .nav-btn svg{width:19px;height:19px; flex-shrink:0;}
  .nav-btn span{font-size:12px; letter-spacing:.2px; font-weight:600; max-width:0; overflow:hidden; white-space:nowrap; transition:max-width .25s ease;}
  .nav-btn.active{color:var(--nav-item-active-color); background:var(--nav-item-active-bg); box-shadow:0 6px 16px rgba(0,0,0,0.18);}
  .nav-btn.active span{max-width:100px; margin-left:1px;}

  /* ---------- Notes ---------- */
  .note-card{
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius-m);
    padding:14px 15px; margin-bottom:10px; cursor:pointer; box-shadow:var(--shadow-soft);
    max-width:100%; overflow:hidden;
  }
  .note-card:active{transform:scale(.985);}
  .note-title{font-size:14.5px; font-weight:700; color:var(--text-hi); margin-bottom:4px;}
  .note-snippet{font-size:12.5px; color:var(--text-mid); line-height:1.55; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden;}
  .note-meta{font-size:10.5px; color:var(--text-low); margin-top:8px; font-family:'DM Mono',monospace;}

  /* ---------- Settings ---------- */
  .settings-card{
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius-l);
    padding:18px; margin-bottom:16px; box-shadow:var(--shadow-soft);
    max-width:100%; overflow:hidden;
  }
  .settings-label{font-size:11px; letter-spacing:1.5px; text-transform:uppercase; color:var(--text-low); margin-bottom:10px; font-weight:600;}
  .field{
    background:var(--surface-inset); border:1px solid var(--border); border-radius:var(--radius-s);
    padding:13px 14px; display:flex; align-items:center; gap:10px;
  }
  .field input{
    background:transparent; border:none; outline:none; color:var(--text-hi); font-size:14.5px; flex:1; min-width:0;
  }
  .field input::placeholder{color:var(--text-low);}
  .field svg{width:16px;height:16px; color:var(--text-low); flex-shrink:0;}
  .save-row{display:flex; justify-content:flex-end; margin-top:12px;}
  .btn{
    border:none; cursor:pointer; border-radius:var(--radius-full);
    padding:11px 20px; font-size:13.5px; font-weight:600; letter-spacing:.2px;
  }
  .btn-primary{background:var(--accent-peach); color:var(--accent-blue-deep); box-shadow:0 8px 18px rgba(224,173,31,0.35); font-weight:700;}
  body.theme-old .btn-primary{background:linear-gradient(135deg, var(--accent-peach), var(--accent-blue)); color:#3a2a20; box-shadow:0 8px 18px rgba(180,130,95,0.3); font-weight:600;}
  .btn-primary:active{transform:scale(.97);}
  .btn-ghost{background:var(--surface-inset); border:1px solid var(--border); color:var(--text-mid);}
  .btn-danger{background:transparent; border:1px solid rgba(207,123,104,0.4); color:var(--danger);}
  .settings-desc{font-size:12.5px; color:var(--text-mid); line-height:1.6; margin-top:10px;}
  .saved-pill{
    display:inline-flex; align-items:center; gap:6px; font-size:11.5px; color:var(--success);
    background:rgba(132,172,134,0.14); border:1px solid rgba(132,172,134,0.35); padding:5px 11px; border-radius:var(--radius-full);
    margin-top:10px; opacity:0; transition:opacity .3s ease;
  }
  .saved-pill.show{opacity:1;}
  .saved-pill svg{width:12px;height:12px;}

  /* ---------- Modal / Sheet ---------- */
  .overlay{
    position:fixed; inset:0; z-index:60; background:rgba(60,42,30,0.28);
    display:flex; align-items:flex-end; justify-content:center;
    opacity:0; pointer-events:none; transition:opacity .25s ease;
  }
  .overlay.open{opacity:1; pointer-events:auto;}
  .sheet{
    width:100%; max-width:480px; background:var(--surface-raised);
    border:1px solid var(--border); border-bottom:none;
    border-radius:26px 26px 0 0; padding:10px 20px calc(24px + env(safe-area-inset-bottom));
    box-shadow:var(--shadow-deep);
    transform:translateY(100%); transition:transform .3s cubic-bezier(.2,.9,.25,1);
    max-height:88vh; overflow-y:auto;
  }
  .overlay.open .sheet{transform:translateY(0);}
  .sheet-handle{width:36px; height:4px; background:var(--border); border-radius:2px; margin:8px auto 16px;}
  .sheet-title{font-family:'Italiana',serif; font-size:21px; font-weight:400; margin-bottom:16px; letter-spacing:.3px;}
  .form-row{margin-bottom:12px;}
  .form-label{font-size:11px; letter-spacing:1.2px; text-transform:uppercase; color:var(--text-low); margin-bottom:7px; display:block; font-weight:600;}
  .type-toggle{display:flex; gap:8px;}
  .type-opt{
    flex:1; text-align:center; padding:11px; border-radius:var(--radius-s);
    border:1px solid var(--border); background:var(--surface-inset); color:var(--text-mid);
    font-size:13px; font-weight:600; cursor:pointer; display:flex; align-items:center; justify-content:center; gap:6px;
  }
  .type-opt svg{width:14px;height:14px; flex-shrink:0;}
  .type-opt.active{border-color:var(--border-accent); color:var(--accent-peach-deep); background:rgba(226,160,119,0.12);}
  .sheet-actions{display:flex; gap:10px; margin-top:18px;}
  .sheet-actions .btn{flex:1;}
  textarea.field-plain{
    width:100%; background:var(--surface-inset); border:1px solid var(--border); border-radius:var(--radius-s);
    padding:13px 14px; color:var(--text-hi); font-size:14px; outline:none; resize:none; min-height:90px; line-height:1.6;
    overflow-wrap:anywhere;
  }
  input.field-plain{
    width:100%; background:var(--surface-inset); border:1px solid var(--border); border-radius:var(--radius-s);
    padding:13px 14px; color:var(--text-hi); font-size:14.5px; outline:none;
  }
  .field-plain::placeholder{color:var(--text-low);}
  .confirm-text{font-size:13.5px; color:var(--text-mid); line-height:1.6; margin-bottom:18px;}

  .toast{
    position:fixed; left:50%; bottom:100px; transform:translateX(-50%) translateY(10px);
    background:var(--surface-raised); border:1px solid var(--border); border-radius:var(--radius-full);
    padding:10px 18px; font-size:12.5px; color:var(--text-hi); box-shadow:var(--shadow-deep);
    z-index:80; opacity:0; transition:opacity .25s ease, transform .25s ease; display:flex; align-items:center; gap:8px;
    pointer-events:none; max-width:88%;
  }
  .toast.show{opacity:1; transform:translateX(-50%) translateY(0);}
  .toast svg{width:14px;height:14px; color:var(--success); flex-shrink:0;}

  /* ---------- Onboarding ---------- */
  #onboarding{
    position:fixed; inset:0; z-index:500;
    display:flex; align-items:center; justify-content:center;
    background:linear-gradient(165deg, var(--bg-1) 0%, var(--bg-2) 55%, var(--bg-3) 100%);
    padding:24px;
    opacity:1; transition:opacity .3s ease;
  }
  #onboarding.hide{opacity:0; pointer-events:none; visibility:hidden;}
  .ob-card{
    width:100%; max-width:380px;
    background:var(--surface-raised); border:1px solid var(--border);
    border-radius:var(--radius-l); padding:34px 26px 26px;
    box-shadow:var(--shadow-deep);
    text-align:center;
    animation: fadeUp .4s ease both;
  }
  .ob-dots{display:flex; justify-content:center; gap:6px; margin-bottom:26px;}
  .ob-dot{width:6px; height:6px; border-radius:3px; background:var(--border); transition:width .2s ease, background .2s ease;}
  .ob-dot.active{background:var(--accent-peach-deep); width:18px;}
  .ob-icon{
    width:76px; height:76px; border-radius:50%;
    background:linear-gradient(135deg, var(--accent-peach), var(--accent-blue));
    display:flex; align-items:center; justify-content:center;
    margin:0 auto 22px; box-shadow:var(--shadow-soft);
  }
  .ob-icon svg{width:34px;height:34px; color:#fff;}
  .ob-title{font-family:'Italiana',serif; font-size:23px; font-weight:400; margin-bottom:11px; letter-spacing:.2px;}
  .ob-desc{font-size:13.5px; color:var(--text-mid); line-height:1.65; margin-bottom:26px;}
  .ob-btn{width:100%; padding:13px;}
  .ob-skip{margin-top:16px; font-size:12.5px; color:var(--text-low); cursor:pointer;}
  .ob-skip:active{opacity:.6;}
</style>
</head>
<body>

<div id="splash">
  <div class="logo-mark">
    <div class="logo-ring"></div>
    <svg viewBox="0 0 88 88" fill="none">
      <defs>
        <radialGradient id="peachGrad" cx="35%" cy="35%" r="75%">
          <stop offset="0" stop-color="#f6c945"/>
          <stop offset="1" stop-color="#e0ad1f"/>
        </radialGradient>
        <radialGradient id="blueGrad" cx="65%" cy="65%" r="75%">
          <stop offset="0" stop-color="#6a4bb0"/>
          <stop offset="1" stop-color="#2d1b5e"/>
        </radialGradient>
      </defs>
      <circle cx="44" cy="44" r="43" fill="#fdf8ef" stroke="rgba(122,90,66,0.12)"/>
      <path d="M44 6a38 38 0 010 76 38 38 0 010-76z" fill="url(#peachGrad)" opacity="0.95"/>
      <path d="M44 6a38 38 0 010 76V6z" fill="url(#blueGrad)" opacity="0.95"/>
      <circle cx="44" cy="44" r="15" fill="#fdf8ef"/>
      <path d="M37 44.5l4.8 4.8L52 39" stroke="#4b2f8a" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </div>
  <div class="splash-word">Good Planner</div>
  <div class="splash-sub">Your day, well kept</div>
  <div class="splash-bar"><i></i></div>
</div>

<div id="onboarding" class="hide">
  <div class="ob-card">
    <div class="ob-dots" id="obDots"></div>
    <div class="ob-icon" id="obIcon"></div>
    <div class="ob-title" id="obTitle"></div>
    <div class="ob-desc" id="obDesc"></div>
    <button class="btn btn-primary ob-btn" id="obAction">Continue</button>
    <div class="ob-skip" id="obSkip">Skip for now</div>
  </div>
</div>

<div id="app">
  <header class="top">
    <div class="top-row">
      <div class="brand">
        <div class="brand-mark">
          <svg viewBox="0 0 34 34" fill="none">
            <defs>
              <radialGradient id="peachGrad2" cx="35%" cy="35%" r="75%">
                <stop offset="0" stop-color="#f6c945"/>
                <stop offset="1" stop-color="#e0ad1f"/>
              </radialGradient>
              <radialGradient id="blueGrad2" cx="65%" cy="65%" r="75%">
                <stop offset="0" stop-color="#6a4bb0"/>
                <stop offset="1" stop-color="#2d1b5e"/>
              </radialGradient>
            </defs>
            <circle cx="17" cy="17" r="16.5" fill="#fdf8ef" stroke="rgba(122,90,66,0.12)"/>
            <path d="M17 1a16 16 0 010 32 16 16 0 010-32z" fill="url(#peachGrad2)"/>
            <path d="M17 1a16 16 0 010 32V1z" fill="url(#blueGrad2)"/>
            <circle cx="17" cy="17" r="6" fill="#fdf8ef"/>
            <path d="M14 17.2l1.9 1.9 4-4.2" stroke="#4b2f8a" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <div class="brand-name">Good Planner</div>
      </div>
      <div class="icon-btn" id="btnSearchDate" title="Jump to date">
        <svg viewBox="0 0 24 24" fill="none"><rect x="3" y="5" width="18" height="16" rx="3" stroke="currentColor" stroke-width="1.6"/><path d="M3 10h18" stroke="currentColor" stroke-width="1.6"/><path d="M8 3v3M16 3v3" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>
      </div>
    </div>
  </header>

  <main>
    <!-- TODAY VIEW -->
    <section id="view-today">
      <div class="date-nav">
        <div class="arrow-btn" id="prevDay"><svg viewBox="0 0 24 24" fill="none"><path d="M15 6l-6 6 6 6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <div class="date-label">
          <div class="date-eyebrow" id="dateEyebrow">Today</div>
          <div class="date-title" id="dateTitle">—</div>
        </div>
        <div class="arrow-btn" id="nextDay"><svg viewBox="0 0 24 24" fill="none"><path d="M9 6l6 6-6 6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      </div>

      <div class="day-summary">
        <div class="ring-wrap">
          <svg width="52" height="52" viewBox="0 0 52 52">
            <circle cx="26" cy="26" r="22" fill="none" stroke="rgba(255,255,255,0.18)" stroke-width="5"/>
            <circle id="ringProgress" cx="26" cy="26" r="22" fill="none" stroke="url(#ringGrad)" stroke-width="5" stroke-linecap="round" stroke-dasharray="138.2" stroke-dashoffset="138.2"/>
            <defs><linearGradient id="ringGrad" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#f6c945"/><stop offset="1" stop-color="#e0ad1f"/></linearGradient></defs>
          </svg>
          <div class="ring-pct" id="ringPct">0%</div>
        </div>
        <div class="summary-text">
          <div class="st-title" id="summaryTitle">Nothing planned yet</div>
          <div class="st-sub" id="summarySub">Tap + to add your first item</div>
        </div>
      </div>

      <div class="section-label">Agenda</div>
      <div class="timeline" id="agendaList"></div>
    </section>

    <!-- NOTES VIEW -->
    <section id="view-notes" style="display:none;">
      <div class="date-title" style="margin:14px 0 18px;">Notes</div>
      <div id="notesList"></div>
    </section>

    <!-- SETTINGS VIEW -->
    <section id="view-settings" style="display:none;">
      <div class="date-title" style="margin:14px 0 18px;">Settings</div>

      <div class="settings-card">
        <div class="settings-label">Appearance</div>
        <div class="theme-row">
          <div class="theme-row-text">
            <div class="theme-row-title">Classic warm theme</div>
            <div class="theme-row-sub">Off = new purple &amp; gold theme · On = original peach &amp; blue theme</div>
          </div>
          <div class="switch" id="themeSwitch"><div class="knob"></div></div>
        </div>
      </div>

      <div class="settings-card">
        <div class="settings-label">Account email</div>
        <div class="field">
          <svg viewBox="0 0 24 24" fill="none"><path d="M3 6h18v12H3z" stroke="currentColor" stroke-width="1.5"/><path d="M3 7l9 6 9-6" stroke="currentColor" stroke-width="1.5"/></svg>
          <input id="emailInput" type="email" placeholder="you@example.com">
        </div>
        <div class="settings-desc">Stored only on this device, only used to personalise your planner. No sign-in, no password, no account created anywhere.</div>
        <div class="save-row">
          <button class="btn btn-primary" id="saveEmail">Save</button>
        </div>
        <div class="saved-pill" id="emailSaved"><svg viewBox="0 0 24 24" fill="none"><path d="M4 12l5 5 11-11" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg>Saved</div>
      </div>

      <div class="settings-card" style="border-color:var(--border-accent);">
        <div class="settings-label">Background alarms not ringing?</div>
        <div class="settings-desc" style="margin-top:0;">Many phones (Oppo, Vivo, Xiaomi, Huawei) block apps from running in the background unless "Autostart" is turned on. This is the #1 reason alarms don't ring when the screen is off or the app is closed. Tap below to open that setting directly.</div>
        <div class="save-row" style="justify-content:flex-start; margin-top:12px;">
          <button class="btn btn-primary" id="fixAutostart">Fix background alarms</button>
        </div>
      </div>

      <div class="settings-card">
        <div class="settings-label">Reminder notifications</div>
        <div class="settings-desc" style="margin-top:0;">Allow notifications so reminders can alert you. In the installed app, reminders ring at their set time even if the app is closed or your phone is locked.</div>
        <div class="save-row" style="justify-content:flex-start; margin-top:12px; gap:10px;">
          <button class="btn btn-ghost" id="enableNotif">Enable notifications</button>
          <button class="btn btn-ghost" id="testAlarm">Test alarm</button>
        </div>
        <div class="save-row" style="justify-content:flex-start; margin-top:10px;">
          <button class="btn btn-ghost" id="reviewPermissions">Review all permissions</button>
        </div>
      </div>

      <div class="settings-card">
        <div class="settings-label">Data</div>
        <div class="settings-desc" style="margin-top:0;">Everything you add — tasks, reminders, notes — lives privately on this device only.</div>
        <div class="save-row" style="justify-content:flex-start; margin-top:12px;">
          <button class="btn btn-danger" id="clearData">Clear all data</button>
        </div>
      </div>

      <div class="settings-card" style="text-align:center; box-shadow:none; background:transparent; border:none;">
        <div style="font-size:11px; color:var(--text-low); letter-spacing:.5px;">Good Planner · v1.4</div>
      </div>
    </section>
  </main>

  <button class="fab" id="fabAdd">
    <svg viewBox="0 0 24 24" fill="none"><path d="M12 5v14M5 12h14" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"/></svg>
  </button>

  <nav class="bottom">
    <div class="nav-inner">
      <div class="nav-btn active" data-view="today">
        <svg viewBox="0 0 24 24" fill="none"><rect x="3" y="4" width="18" height="17" rx="4" stroke="currentColor" stroke-width="1.7"/><path d="M3 9h18" stroke="currentColor" stroke-width="1.7"/></svg>
        <span>Today</span>
      </div>
      <div class="nav-btn" data-view="notes">
        <svg viewBox="0 0 24 24" fill="none"><path d="M5 4h11l3 3v13H5z" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><path d="M9 10h6M9 14h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
        <span>Notes</span>
      </div>
      <div class="nav-btn" data-view="settings">
        <svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="1.7"/><path d="M19.4 13.5a1.7 1.7 0 000-3l1-1.7-1.7-1.7-1.7 1a1.7 1.7 0 00-3 0l-1-1.7-1.7 0-1 1.7a1.7 1.7 0 00-3 0l-1.7-1L3.6 8.8l1 1.7a1.7 1.7 0 000 3l-1 1.7 1.7 1.7 1.7-1a1.7 1.7 0 003 0l1 1.7h1.7l1-1.7a1.7 1.7 0 003 0l1.7 1 1.7-1.7z" stroke="currentColor" stroke-width="1.1" stroke-linejoin="round"/></svg>
        <span>Settings</span>
      </div>
    </div>
  </nav>
</div>

<!-- Add / Edit item sheet -->
<div class="overlay" id="addOverlay">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-title" id="itemSheetTitle">Add to your day</div>
    <div class="form-row">
      <span class="form-label">Type</span>
      <div class="type-toggle">
        <div class="type-opt active" id="typeTask" data-type="task">
          <svg viewBox="0 0 24 24" fill="none"><path d="M4 12l5 5 11-11" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Task
        </div>
        <div class="type-opt" id="typeReminder" data-type="reminder">
          <svg viewBox="0 0 24 24" fill="none"><path d="M12 3a5 5 0 00-5 5v3.5c0 .7-.28 1.4-.78 1.9L5 15h14l-1.22-1.6a2.7 2.7 0 01-.78-1.9V8a5 5 0 00-5-5z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/><path d="M9.5 18a2.5 2.5 0 005 0" stroke="currentColor" stroke-width="1.6"/></svg>
          Reminder
        </div>
      </div>
    </div>
    <div class="form-row">
      <span class="form-label">Title</span>
      <input class="field-plain" id="itemTitle" placeholder="e.g. Call the supplier">
    </div>
    <div class="form-row">
      <span class="form-label">Time</span>
      <input class="field-plain" id="itemTime" type="time">
    </div>
    <div class="form-row">
      <span class="form-label">Description <span style="text-transform:none; letter-spacing:0; font-weight:400; color:var(--text-low);">(what is this for)</span></span>
      <textarea class="field-plain" id="itemDesc" placeholder="Add a few details so you remember exactly what this is for..."></textarea>
    </div>
    <div class="form-row">
      <span class="form-label">Repeat</span>
      <div class="type-toggle">
        <div class="type-opt active" id="repeatOnce" data-repeat="once">
          <svg viewBox="0 0 24 24" fill="none"><rect x="4" y="5" width="16" height="15" rx="2" stroke="currentColor" stroke-width="1.6"/><path d="M4 9h16" stroke="currentColor" stroke-width="1.6"/></svg>
          This date only
        </div>
        <div class="type-opt" id="repeatDaily" data-repeat="daily">
          <svg viewBox="0 0 24 24" fill="none"><path d="M4 12a8 8 0 0113.66-5.66M20 12a8 8 0 01-13.66 5.66" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/><path d="M18 3v4h-4M6 21v-4h4" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Every day
        </div>
      </div>
    </div>
    <div class="sheet-actions">
      <button class="btn btn-danger" id="deleteItem" style="display:none;">Delete</button>
      <button class="btn btn-ghost" id="cancelAdd">Cancel</button>
      <button class="btn btn-primary" id="confirmAdd">Add</button>
    </div>
  </div>
</div>

<!-- Note editor sheet -->
<div class="overlay" id="noteOverlay">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-title" id="noteSheetTitle">New note</div>
    <div class="form-row">
      <input class="field-plain" id="noteTitleInput" placeholder="Title">
    </div>
    <div class="form-row">
      <textarea class="field-plain" id="noteBodyInput" placeholder="Write something..." style="min-height:140px;"></textarea>
    </div>
    <div class="sheet-actions">
      <button class="btn btn-danger" id="deleteNote" style="display:none;">Delete</button>
      <button class="btn btn-ghost" id="cancelNote">Cancel</button>
      <button class="btn btn-primary" id="saveNote">Save</button>
    </div>
  </div>
</div>

<!-- Confirm sheet -->
<div class="overlay" id="confirmOverlay">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-title">Clear all data?</div>
    <div class="confirm-text">This removes every task, reminder, note and your saved email from this device. This can't be undone.</div>
    <div class="sheet-actions">
      <button class="btn btn-ghost" id="cancelClear">Cancel</button>
      <button class="btn btn-danger" id="confirmClear" style="flex:1; background:var(--danger); color:#fff; border:none;">Clear everything</button>
    </div>
  </div>
</div>

<div class="toast" id="toast"><svg viewBox="0 0 24 24" fill="none"><path d="M4 12l5 5 11-11" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg><span id="toastText" class="wrap-text">Saved</span></div>

<script>
(function(){
  const $ = (s)=>document.querySelector(s);
  const $$ = (s)=>Array.from(document.querySelectorAll(s));

  let currentDate = new Date();
  currentDate.setHours(0,0,0,0);
  let dayCache = {}; // 'YYYY-MM-DD' -> {tasks:[...], dailyDone:{...}}
  let dailyCache = null; // array of daily-repeating item templates, shared across all days
  let notesCache = [];
  let editingNoteId = null;
  let editingItemId = null; // when set, add sheet is in edit mode
  let editingIsDaily = false; // whether the item being edited lives in the daily template list
  let pendingType = 'task';
  let pendingRepeat = 'once';

  function fmtKey(d){
    return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');
  }
  function isSameDay(a,b){ return fmtKey(a)===fmtKey(b); }
  function uid(){ return Date.now().toString(36)+Math.random().toString(36).slice(2,7); }

  // ---------- Native alarm engine (real Android alarm, not a plain notification) ----------
  // "Native" is our own AlarmScheduler plugin (built from native-alarm/alarm/*.java in this
  // repo) — it hands reminders off to Android's AlarmManager, which fires a full-screen,
  // looping alarm sound even if the app is closed or the phone is locked, exactly like the
  // phone's own Clock app. "NotifPerm" is only the official @capacitor/local-notifications
  // plugin, used purely to trigger the standard Android 13+ notification permission prompt.
  const Native = (window.Capacitor && window.Capacitor.isNativePlatform && window.Capacitor.isNativePlatform() && window.Capacitor.Plugins && window.Capacitor.Plugins.AlarmScheduler)
    ? window.Capacitor.Plugins.AlarmScheduler
    : null;
  const NotifPerm = (window.Capacitor && window.Capacitor.Plugins && window.Capacitor.Plugins.LocalNotifications)
    ? window.Capacitor.Plugins.LocalNotifications
    : null;

  function hashId(str){
    let h = 0;
    for(let i=0;i<str.length;i++){ h = (h*31 + str.charCodeAt(i)) | 0; }
    return Math.abs(h) % 2147483000 + 1;
  }

  function buildDateTime(dateObj, timeStr){
    const [h,m] = timeStr.split(':').map(Number);
    const d = new Date(dateObj);
    d.setHours(h, m, 0, 0);
    return d;
  }

  async function ensureNotifChannel(){
    if(!NotifPerm) return true; // no permission plugin available — assume fine (older Android)
    try{
      const perm = await NotifPerm.checkPermissions();
      if(perm.display === 'granted') return true;
      const req = await NotifPerm.requestPermissions();
      return req.display === 'granted';
    }catch(e){ return true; }
  }

  async function scheduleNativeReminder(item, dateObj){
    if(!Native || item.type!=='reminder' || !item.time) return;
    const granted = await ensureNotifChannel();
    if(!granted){ showToast('Enable notification permission to get reminder alarms'); return; }
    const isDaily = item.repeat==='daily';
    let atDate = new Date(dateObj);
    if(atDate.getTime() <= Date.now()){
      if(isDaily){ atDate = new Date(atDate.getTime() + 86400000); } // first ring tomorrow if today's time already passed
      else { return; } // one-off reminder in the past — don't schedule
    }
    try{
      await Native.schedule({
        id: hashId(item.id),
        atMillis: atDate.getTime(),
        title: item.title || 'Reminder',
        body: item.desc || 'Your reminder time has arrived',
        repeatDaily: isDaily,
        hour: atDate.getHours(),
        minute: atDate.getMinutes()
      });
    }catch(e){ console.error('native schedule failed', e); }
  }

  async function cancelNativeReminder(itemId){
    if(!Native) return;
    try{ await Native.cancel({ id: hashId(itemId) }); }catch(e){}
  }

  // Cancels the alarm for every reminder currently stored (used before wiping all data).
  async function cancelAllNativeReminders(){
    if(!Native) return;
    for(const item of loadDailyItems()){
      if(item.type==='reminder') await cancelNativeReminder(item.id);
    }
    const keys = storListPrefix('day:');
    for(const k of keys){
      const day = storGet(k);
      if(!day || !day.tasks) continue;
      for(const item of day.tasks){
        if(item.type==='reminder') await cancelNativeReminder(item.id);
      }
    }
  }

  // On startup, re-schedule any not-yet-fired future reminders (today or later),
  // plus every daily-repeating reminder template. This matters after
  // reinstall/update, since past AlarmManager schedules don't survive that.
  async function resyncNativeReminders(){
    if(!Native) return;
    for(const item of loadDailyItems()){
      if(item.type==='reminder' && item.time){
        await scheduleNativeReminder(item, buildDateTime(new Date(), item.time));
      }
    }
    const keys = storListPrefix('day:');
    const todayStr = fmtKey(new Date());
    for(const k of keys){
      const dateStr = k.slice(4); // strip 'day:' prefix
      if(dateStr < todayStr) continue;
      const day = storGet(k);
      if(!day || !day.tasks) continue;
      day.tasks.forEach(item=>{
        if(item.type==='reminder' && item.time && !item.done){
          scheduleNativeReminder(item, buildDateTime(new Date(dateStr+'T00:00:00'), item.time));
        }
      });
    }
  }

  // ---------- storage helpers (localStorage based, works fully offline in an APK) ----------
  const STORAGE_PREFIX = 'goodplanner:';

  function storGet(key){
    try{
      const raw = localStorage.getItem(STORAGE_PREFIX+key);
      return raw ? JSON.parse(raw) : null;
    }catch(e){ return null; }
  }
  function storSet(key, val){
    try{ localStorage.setItem(STORAGE_PREFIX+key, JSON.stringify(val)); }catch(e){ console.error(e); }
  }
  function storDelete(key){
    try{ localStorage.removeItem(STORAGE_PREFIX+key); }catch(e){}
  }
  function storListPrefix(prefix){
    try{
      const full = STORAGE_PREFIX+prefix;
      const keys = [];
      for(let i=0;i<localStorage.length;i++){
        const k = localStorage.key(i);
        if(k && k.indexOf(full)===0){ keys.push(k.slice(STORAGE_PREFIX.length)); }
      }
      return keys;
    }catch(e){ return []; }
  }

  function loadDay(dateKey){
    if(dayCache[dateKey]) return dayCache[dateKey];
    const data = storGet('day:'+dateKey);
    dayCache[dateKey] = data || {tasks:[], dailyDone:{}};
    if(!dayCache[dateKey].dailyDone) dayCache[dateKey].dailyDone = {};
    return dayCache[dateKey];
  }
  function saveDay(dateKey){
    storSet('day:'+dateKey, dayCache[dateKey]);
  }

  // Daily-repeating item templates — one shared list, shown on every day's agenda.
  function loadDailyItems(){
    if(dailyCache) return dailyCache;
    dailyCache = storGet('daily') || [];
    return dailyCache;
  }
  function saveDailyItems(items){
    dailyCache = items;
    storSet('daily', items);
  }

  function loadNotes(){
    const data = storGet('notes');
    notesCache = data || [];
  }
  function saveNotes(){
    storSet('notes', notesCache);
  }

  // ---------- rendering: today view ----------
  const monthNames=['January','February','March','April','May','June','July','August','September','October','November','December'];
  const dayNames=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

  function updateDateHeader(){
    const today = new Date(); today.setHours(0,0,0,0);
    const eyebrow = isSameDay(currentDate, today) ? 'Today' :
      (isSameDay(currentDate, new Date(today.getTime()+86400000)) ? 'Tomorrow' :
      (isSameDay(currentDate, new Date(today.getTime()-86400000)) ? 'Yesterday' : dayNames[currentDate.getDay()]));
    $('#dateEyebrow').textContent = eyebrow;
    $('#dateTitle').textContent = currentDate.getDate()+' '+monthNames[currentDate.getMonth()]+', '+currentDate.getFullYear();
  }

  function timeToMinutes(t){
    if(!t) return 9999;
    const [h,m] = t.split(':').map(Number);
    return h*60+m;
  }
  function fmtTime(t){
    if(!t) return '--:--';
    let [h,m] = t.split(':').map(Number);
    const ap = h>=12?'PM':'AM';
    let hh = h%12; if(hh===0) hh=12;
    return hh+':'+String(m).padStart(2,'0')+' '+ap;
  }

  function renderToday(){
    updateDateHeader();
    const key = fmtKey(currentDate);
    const day = loadDay(key);
    const dailyItems = loadDailyItems().map(d => ({
      ...d,
      done: !!day.dailyDone[d.id],
      _isDaily: true
    }));
    const onceItems = day.tasks.map(t => ({ ...t, _isDaily:false }));
    const items = [...onceItems, ...dailyItems].sort((a,b)=>timeToMinutes(a.time)-timeToMinutes(b.time));
    const list = $('#agendaList');
    list.innerHTML='';

    const total = items.length;
    const doneCount = items.filter(i=>i.done).length;
    const pct = total? Math.round((doneCount/total)*100) : 0;
    const circumference = 138.2;
    $('#ringProgress').style.strokeDashoffset = circumference - (circumference*pct/100);
    $('#ringPct').textContent = pct+'%';
    if(total===0){
      $('#summaryTitle').textContent = 'Nothing planned yet';
      $('#summarySub').textContent = 'Tap + to add your first item';
    } else {
      $('#summaryTitle').textContent = doneCount+' of '+total+' done';
      const reminders = items.filter(i=>i.type==='reminder').length;
      $('#summarySub').textContent = reminders? reminders+' reminder'+(reminders>1?'s':'')+' set' : 'Keep going';
    }

    if(items.length===0){
      list.innerHTML = `<div class="empty-state">
        <svg viewBox="0 0 24 24" fill="none"><rect x="3" y="4" width="18" height="17" rx="4" stroke="currentColor" stroke-width="1.4"/><path d="M3 9h18" stroke="currentColor" stroke-width="1.4"/></svg>
        <div class="es-title">A clean slate</div>
        <div class="es-sub">Add a task or reminder to get this day moving.</div>
      </div>`;
      return;
    }

    items.forEach(item=>{
      const dailyAttr = item._isDaily ? '1' : '0';
      const el = document.createElement('div');
      el.className = 'agenda-item'+(item.done?' done':'')+(item.type==='reminder'?' reminder':'');
      el.innerHTML = `
        <div class="time-col"><div class="time-text">${fmtTime(item.time)}</div></div>
        <div class="node"></div>
        <div class="item-card" data-id="${item.id}" data-daily="${dailyAttr}">
          ${item.type==='task' ? `<div class="check ${item.done?'checked':''}" data-id="${item.id}" data-daily="${dailyAttr}"><svg viewBox="0 0 24 24" fill="none"><path d="M4 12l5 5 11-11" stroke="#f8efe2" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/></svg></div>` : ''}
          <div class="item-body">
            <div class="item-title wrap-text">${escapeHtml(item.title)}</div>
            ${item.desc ? `<div class="item-desc wrap-text">${escapeHtml(item.desc)}</div>` : ''}
            ${item.type==='reminder' ? `<div class="item-tag"><svg viewBox="0 0 24 24" fill="none"><path d="M12 3a5 5 0 00-5 5v3.5c0 .7-.28 1.4-.78 1.9L5 15h14l-1.22-1.6a2.7 2.7 0 01-.78-1.9V8a5 5 0 00-5-5z" stroke="currentColor" stroke-width="1.6"/></svg>${item.repeat==='daily'?'Reminder · Every day':'Reminder'}</div>` : (item.repeat==='daily' ? `<div class="item-tag"><svg viewBox="0 0 24 24" fill="none"><path d="M4 12a8 8 0 0113.66-5.66M20 12a8 8 0 01-13.66 5.66" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>Every day</div>` : '')}
          </div>
          <div class="del-btn" data-id="${item.id}" data-daily="${dailyAttr}"><svg viewBox="0 0 24 24" fill="none"><path d="M6 6l12 12M18 6L6 18" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg></div>
        </div>`;
      list.appendChild(el);
    });

    $$('.check').forEach(c=>c.addEventListener('click', (e)=>{
      e.stopPropagation();
      const id = e.currentTarget.getAttribute('data-id');
      const isDaily = e.currentTarget.getAttribute('data-daily')==='1';
      const dayKey = fmtKey(currentDate);
      if(isDaily){
        const d = loadDay(dayKey);
        d.dailyDone[id] = !d.dailyDone[id];
        saveDay(dayKey);
      } else {
        const d = loadDay(dayKey);
        const t = d.tasks.find(x=>x.id===id);
        if(t) t.done = !t.done;
        saveDay(dayKey);
      }
      renderToday();
    }));
    $$('.del-btn').forEach(c=>c.addEventListener('click', async (e)=>{
      e.stopPropagation();
      const id = e.currentTarget.getAttribute('data-id');
      const isDaily = e.currentTarget.getAttribute('data-daily')==='1';
      if(isDaily){
        saveDailyItems(loadDailyItems().filter(x=>x.id!==id));
      } else {
        const dayKey = fmtKey(currentDate);
        const d = loadDay(dayKey);
        d.tasks = d.tasks.filter(x=>x.id!==id);
        saveDay(dayKey);
      }
      await cancelNativeReminder(id);
      renderToday();
      showToast('Removed');
    }));
    $$('.item-card').forEach(c=>c.addEventListener('click', (e)=>{
      const id = e.currentTarget.getAttribute('data-id');
      const isDaily = e.currentTarget.getAttribute('data-daily')==='1';
      if(isDaily){
        const item = loadDailyItems().find(x=>x.id===id);
        if(item) openItemEditor(item, true);
      } else {
        const d = loadDay(fmtKey(currentDate));
        const item = d.tasks.find(x=>x.id===id);
        if(item) openItemEditor(item, false);
      }
    }));
  }

  function escapeHtml(s){
    return s.replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }

  // ---------- Notes ----------
  function renderNotes(){
    const list = $('#notesList');
    list.innerHTML='';
    if(notesCache.length===0){
      list.innerHTML = `<div class="empty-state">
        <svg viewBox="0 0 24 24" fill="none"><path d="M5 4h11l3 3v13H5z" stroke="currentColor" stroke-width="1.4"/></svg>
        <div class="es-title">No notes yet</div>
        <div class="es-sub">Tap + to jot something down.</div>
      </div>`;
      return;
    }
    [...notesCache].sort((a,b)=>b.updated-a.updated).forEach(n=>{
      const el = document.createElement('div');
      el.className='note-card';
      el.innerHTML = `<div class="note-title wrap-text">${escapeHtml(n.title||'Untitled')}</div>
        <div class="note-snippet wrap-text">${escapeHtml(n.body||'')}</div>
        <div class="note-meta">${new Date(n.updated).toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'})}</div>`;
      el.addEventListener('click', ()=>openNoteEditor(n.id));
      list.appendChild(el);
    });
  }

  function openNoteEditor(id){
    editingNoteId = id||null;
    const n = id ? notesCache.find(x=>x.id===id) : null;
    $('#noteSheetTitle').textContent = id ? 'Edit note' : 'New note';
    $('#noteTitleInput').value = n? n.title : '';
    $('#noteBodyInput').value = n? n.body : '';
    $('#deleteNote').style.display = id ? 'block' : 'none';
    openOverlay('#noteOverlay');
  }

  $('#saveNote').addEventListener('click', ()=>{
    const title = $('#noteTitleInput').value.trim();
    const body = $('#noteBodyInput').value.trim();
    if(!title && !body){ closeOverlay('#noteOverlay'); return; }
    if(editingNoteId){
      const n = notesCache.find(x=>x.id===editingNoteId);
      n.title=title; n.body=body; n.updated=Date.now();
    } else {
      notesCache.push({id:uid(), title, body, updated:Date.now()});
    }
    saveNotes();
    closeOverlay('#noteOverlay');
    renderNotes();
    showToast('Note saved');
  });
  $('#deleteNote').addEventListener('click', ()=>{
    notesCache = notesCache.filter(x=>x.id!==editingNoteId);
    saveNotes();
    closeOverlay('#noteOverlay');
    renderNotes();
    showToast('Note deleted');
  });
  $('#cancelNote').addEventListener('click', ()=>closeOverlay('#noteOverlay'));

  // ---------- Add / Edit item sheet ----------
  $('#typeTask').addEventListener('click', ()=>setType('task'));
  $('#typeReminder').addEventListener('click', ()=>setType('reminder'));
  function setType(t){
    pendingType = t;
    $('#typeTask').classList.toggle('active', t==='task');
    $('#typeReminder').classList.toggle('active', t==='reminder');
  }
  $('#repeatOnce').addEventListener('click', ()=>setRepeat('once'));
  $('#repeatDaily').addEventListener('click', ()=>setRepeat('daily'));
  function setRepeat(r){
    pendingRepeat = r;
    $('#repeatOnce').classList.toggle('active', r==='once');
    $('#repeatDaily').classList.toggle('active', r==='daily');
  }
  $('#fabAdd').addEventListener('click', ()=>{
    const activeView = $('.nav-btn.active').getAttribute('data-view');
    if(activeView==='notes'){ openNoteEditor(null); return; }
    if(activeView==='settings'){ switchView('today'); }
    openItemEditor(null, false);
  });

  function openItemEditor(item, isDaily){
    editingItemId = item ? item.id : null;
    editingIsDaily = !!isDaily;
    $('#itemSheetTitle').textContent = item ? 'Edit item' : 'Add to your day';
    $('#itemTitle').value = item ? item.title : '';
    $('#itemTime').value = item ? (item.time||'') : '';
    $('#itemDesc').value = item ? (item.desc||'') : '';
    setType(item ? item.type : 'task');
    setRepeat(item && item.repeat==='daily' ? 'daily' : 'once');
    $('#deleteItem').style.display = item ? 'block' : 'none';
    $('#confirmAdd').textContent = item ? 'Save' : 'Add';
    openOverlay('#addOverlay');
  }

  $('#cancelAdd').addEventListener('click', ()=>closeOverlay('#addOverlay'));
  $('#confirmAdd').addEventListener('click', async ()=>{
    const title = $('#itemTitle').value.trim();
    if(!title){ $('#itemTitle').focus(); return; }
    const time = $('#itemTime').value;
    const desc = $('#itemDesc').value.trim();
    const dayKey = fmtKey(currentDate);

    if(editingItemId){
      // Remove from wherever it currently lives — its repeat setting may have changed.
      if(editingIsDaily){
        saveDailyItems(loadDailyItems().filter(x=>x.id!==editingItemId));
      } else {
        const d = loadDay(dayKey);
        d.tasks = d.tasks.filter(x=>x.id!==editingItemId);
        saveDay(dayKey);
      }
      await cancelNativeReminder(editingItemId);

      const updated = {id:editingItemId, title, time, desc, type:pendingType, repeat:pendingRepeat, done:false};
      if(pendingRepeat==='daily'){
        const items = loadDailyItems();
        items.push(updated);
        saveDailyItems(items);
      } else {
        const d = loadDay(dayKey);
        d.tasks.push(updated);
        saveDay(dayKey);
      }
      if(pendingType==='reminder' && time){
        await scheduleNativeReminder(updated, pendingRepeat==='daily' ? buildDateTime(new Date(), time) : buildDateTime(currentDate, time));
      }
      closeOverlay('#addOverlay');
      renderToday();
      showToast('Saved');
    } else {
      const newItem = {id:uid(), title, time, desc, type:pendingType, repeat:pendingRepeat, done:false, alerted:false};
      if(pendingRepeat==='daily'){
        const items = loadDailyItems();
        items.push(newItem);
        saveDailyItems(items);
      } else {
        const d = loadDay(dayKey);
        d.tasks.push(newItem);
        saveDay(dayKey);
      }
      if(pendingType==='reminder' && time){
        await scheduleNativeReminder(newItem, pendingRepeat==='daily' ? buildDateTime(new Date(), time) : buildDateTime(currentDate, time));
      }
      closeOverlay('#addOverlay');
      renderToday();
      const label = pendingRepeat==='daily' ? ' (every day)' : '';
      showToast((pendingType==='reminder' ? 'Reminder added' : 'Task added') + label);
    }
  });
  $('#deleteItem').addEventListener('click', async ()=>{
    if(!editingItemId) return;
    if(editingIsDaily){
      saveDailyItems(loadDailyItems().filter(x=>x.id!==editingItemId));
    } else {
      const dayKey = fmtKey(currentDate);
      const d = loadDay(dayKey);
      d.tasks = d.tasks.filter(x=>x.id!==editingItemId);
      saveDay(dayKey);
    }
    await cancelNativeReminder(editingItemId);
    closeOverlay('#addOverlay');
    renderToday();
    showToast('Removed');
  });

  // ---------- Onboarding: welcome + step-by-step permission requests ----------
  const OB_ICONS = {
    welcome: `<svg viewBox="0 0 24 24" fill="none"><path d="M4 12l5 5 11-11" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    bell: `<svg viewBox="0 0 24 24" fill="none"><path d="M12 3a5 5 0 00-5 5v3.5c0 .7-.28 1.4-.78 1.9L5 15h14l-1.22-1.6a2.7 2.7 0 01-.78-1.9V8a5 5 0 00-5-5z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/><path d="M9.5 18a2.5 2.5 0 005 0" stroke="currentColor" stroke-width="1.8"/></svg>`,
    alarm: `<svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="13" r="7.5" stroke="currentColor" stroke-width="1.8"/><path d="M12 9.5V13l3 2" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><path d="M5 3L2.5 5.5M19 3l2.5 2.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>`,
    battery: `<svg viewBox="0 0 24 24" fill="none"><rect x="3" y="7" width="16" height="11" rx="2" stroke="currentColor" stroke-width="1.8"/><path d="M20 10.5v4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/><path d="M12.5 10l-2.5 3.2h2.8L10.3 16" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    check: `<svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.8"/><path d="M8.2 12.3l2.4 2.4 5-5.2" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    autostart: `<svg viewBox="0 0 24 24" fill="none"><path d="M12 3v7" stroke="currentColor" stroke-width="1.9" stroke-linecap="round"/><path d="M7 5.5a8 8 0 105 -.1" stroke="currentColor" stroke-width="1.9" stroke-linecap="round"/></svg>`
  };

  function buildOnboardingSteps(){
    const steps = [{
      icon: OB_ICONS.welcome,
      title: 'Welcome to Good Planner',
      desc: "Let's set up a couple of quick permissions so your tasks and reminders always work exactly as they should.",
      action: 'Get Started', skip: false
    }, {
      icon: OB_ICONS.bell,
      title: 'Stay notified',
      desc: 'Allow notifications so Good Planner can alert you the moment a reminder is due.',
      action: 'Allow notifications', skip: true,
      handler: async ()=>{ await ensureNotifChannel(); }
    }];
    if(Native){
      steps.push({
        icon: OB_ICONS.alarm,
        title: 'Ring exactly on time',
        desc: 'Grant alarm permission so reminders ring at the precise moment you set — not late, never skipped.',
        action: 'Allow alarms', skip: true,
        handler: async ()=>{
          try{
            const check = await Native.canScheduleExact();
            if(check && check.value === false){ await Native.openExactAlarmSettings(); }
          }catch(e){}
        }
      });
      steps.push({
        icon: OB_ICONS.battery,
        title: 'Keep reminders alive',
        desc: 'Some phones aggressively close background apps. Allow Good Planner to run unrestricted so alarms still ring even when the app is closed.',
        action: 'Allow', skip: true,
        handler: async ()=>{
          try{
            const check = await Native.isIgnoringBatteryOptimizations();
            if(check && check.value === false){ await Native.requestIgnoreBatteryOptimizations(); }
          }catch(e){}
        }
      });
      steps.push({
        icon: OB_ICONS.autostart,
        title: 'One more important step',
        desc: 'On phones like Oppo, Vivo, Xiaomi and Huawei, this is the setting that actually decides if alarms ring in the background. Find "Good Planner" on the next screen and turn Autostart ON.',
        action: 'Open Autostart settings', skip: true,
        handler: async ()=>{ try{ await Native.openAutostartSettings(); }catch(e){} }
      });
    }
    steps.push({
      icon: OB_ICONS.check,
      title: "You're all set",
      desc: 'You can always revisit these permissions later from Settings.',
      action: 'Start planning', skip: false
    });
    return steps;
  }

  let OB_STEPS = [];
  let obIndex = 0;

  function renderOnboardingStep(){
    const step = OB_STEPS[obIndex];
    if(!step) return;
    $('#obIcon').innerHTML = step.icon;
    $('#obTitle').textContent = step.title;
    $('#obDesc').textContent = step.desc;
    $('#obAction').textContent = step.action;
    $('#obSkip').style.display = step.skip ? 'block' : 'none';
    $('#obDots').innerHTML = OB_STEPS.map((_,i)=>`<div class="ob-dot ${i===obIndex?'active':''}"></div>`).join('');
  }

  async function advanceOnboarding(){
    obIndex++;
    if(obIndex >= OB_STEPS.length){
      storSet('onboarded', true);
      $('#onboarding').classList.add('hide');
    } else {
      renderOnboardingStep();
    }
  }

  $('#obAction').addEventListener('click', async ()=>{
    const step = OB_STEPS[obIndex];
    if(step && step.handler){ await step.handler(); }
    advanceOnboarding();
  });
  $('#obSkip').addEventListener('click', ()=>advanceOnboarding());
  $('#reviewPermissions').addEventListener('click', ()=>{
    OB_STEPS = buildOnboardingSteps();
    obIndex = 1; // skip the welcome card since they've already used the app
    renderOnboardingStep();
    $('#onboarding').classList.remove('hide');
  });

  function maybeShowOnboarding(){
    if(storGet('onboarded')) return;
    OB_STEPS = buildOnboardingSteps();
    obIndex = 0;
    renderOnboardingStep();
    $('#onboarding').classList.remove('hide');
  }
  function openOverlay(sel){ $(sel).classList.add('open'); }
  function closeOverlay(sel){ $(sel).classList.remove('open'); }
  $$('.overlay').forEach(o=>o.addEventListener('click', (e)=>{ if(e.target===o) o.classList.remove('open'); }));

  let toastTimer;
  function showToast(msg){
    $('#toastText').textContent = msg;
    $('#toast').classList.add('show');
    clearTimeout(toastTimer);
    toastTimer = setTimeout(()=>$('#toast').classList.remove('show'), 1800);
  }

  // ---------- nav ----------
  function switchView(view){
    $$('.nav-btn').forEach(b=>b.classList.toggle('active', b.getAttribute('data-view')===view));
    $('#view-today').style.display = view==='today'?'block':'none';
    $('#view-notes').style.display = view==='notes'?'block':'none';
    $('#view-settings').style.display = view==='settings'?'block':'none';
  }
  $$('.nav-btn').forEach(b=>b.addEventListener('click', ()=>switchView(b.getAttribute('data-view'))));

  $('#prevDay').addEventListener('click', ()=>{
    currentDate = new Date(currentDate.getTime()-86400000);
    renderToday();
  });
  $('#nextDay').addEventListener('click', ()=>{
    currentDate = new Date(currentDate.getTime()+86400000);
    renderToday();
  });
  $('#btnSearchDate').addEventListener('click', ()=>{
    const input = document.createElement('input');
    input.type='date'; input.style.position='fixed'; input.style.opacity='0'; input.style.pointerEvents='none';
    input.value = fmtKey(currentDate);
    document.body.appendChild(input);
    input.addEventListener('change', ()=>{
      if(input.value){ currentDate = new Date(input.value+'T00:00:00'); renderToday(); }
      document.body.removeChild(input);
    });
    input.click();
  });

  // ---------- theme ----------
  function applyTheme(isOld){
    document.body.classList.toggle('theme-old', isOld);
    $('#themeSwitch').classList.toggle('on', isOld);
  }
  function initTheme(){
    const saved = storGet('theme'); // 'old' or 'new' — default is new
    const isOld = saved === 'old';
    applyTheme(isOld);
  }
  $('#themeSwitch').addEventListener('click', ()=>{
    const isOld = !document.body.classList.contains('theme-old');
    applyTheme(isOld);
    storSet('theme', isOld ? 'old' : 'new');
    showToast(isOld ? 'Classic theme on' : 'Purple theme on');
  });

  // ---------- settings ----------
  function initSettings(){
    const profile = storGet('profile');
    if(profile && profile.email) $('#emailInput').value = profile.email;
  }
  $('#saveEmail').addEventListener('click', ()=>{
    const email = $('#emailInput').value.trim();
    storSet('profile', {email});
    $('#emailSaved').classList.add('show');
    setTimeout(()=>$('#emailSaved').classList.remove('show'), 2000);
    showToast('Email saved');
  });
  $('#fixAutostart').addEventListener('click', async ()=>{
    if(!Native){ showToast('Only available in the installed app'); return; }
    try{
      await Native.openAutostartSettings();
      showToast('Look for Good Planner and turn Autostart ON');
    }catch(e){ showToast('Could not open settings on this phone'); }
  });
  $('#enableNotif').addEventListener('click', async ()=>{
    if(Native){
      const granted = await ensureNotifChannel();
      if(granted){
        try{
          const check = await Native.canScheduleExact();
          if(check && check.value === false){
            showToast('Opening alarm permission settings...');
            await Native.openExactAlarmSettings();
            return;
          }
        }catch(e){}
        showToast('Notifications enabled for reminders');
      } else {
        showToast('Permission not granted');
      }
      return;
    }
    if(!('Notification' in window)){ showToast('Not supported on this browser'); return; }
    const perm = await Notification.requestPermission();
    showToast(perm==='granted' ? 'Notifications enabled' : 'Permission not granted');
  });
  $('#testAlarm').addEventListener('click', async ()=>{
    unlockAudio();
    if(Native){
      const granted = await ensureNotifChannel();
      if(!granted){ showToast('Permission not granted'); return; }
      try{
        await Native.schedule({ id: 999999, atMillis: Date.now()+5000, title:'Test alarm', body:'This is what your reminders will sound like', repeatDaily:false });
        showToast('Test alarm in 5 seconds — try locking your phone');
      }catch(e){ showToast('Could not schedule test alarm'); }
    } else {
      fireReminderAlert({title:'Test alarm', desc:'This is what your reminders will sound like'});
    }
  });
  $('#clearData').addEventListener('click', ()=>openOverlay('#confirmOverlay'));
  $('#cancelClear').addEventListener('click', ()=>closeOverlay('#confirmOverlay'));
  $('#confirmClear').addEventListener('click', async ()=>{
    await cancelAllNativeReminders();
    const dayKeys = storListPrefix('day:');
    for(const k of dayKeys){ storDelete(k); }
    storDelete('notes');
    storDelete('profile');
    storDelete('theme');
    storDelete('daily');
    storDelete('onboarded');
    dayCache = {}; notesCache = []; dailyCache = null;
    $('#emailInput').value='';
    applyTheme(false);
    closeOverlay('#confirmOverlay');
    renderToday(); renderNotes();
    showToast('All data cleared');
  });

  // ---------- browser-fallback reminder alarm engine ----------
  // Only used when NOT running as the native app (Native is null) — e.g. testing
  // this page directly in a mobile/desktop browser. Only works while the tab
  // stays open, since no web page can wake a locked phone or closed app.
  let sharedAudioCtx = null;
  function unlockAudio(){
    if(!sharedAudioCtx){
      try{ sharedAudioCtx = new (window.AudioContext || window.webkitAudioContext)(); }catch(e){ return; }
    }
    if(sharedAudioCtx.state === 'suspended'){ sharedAudioCtx.resume().catch(()=>{}); }
  }
  document.addEventListener('click', unlockAudio, {once:false});
  document.addEventListener('touchstart', unlockAudio, {once:false});

  function playAlarmSound(){
    try{
      if(!sharedAudioCtx){ sharedAudioCtx = new (window.AudioContext || window.webkitAudioContext)(); }
      const ctx = sharedAudioCtx;
      const startBeeps = ()=>{
        const beepAt = (start, freq)=>{
          const osc = ctx.createOscillator();
          const gain = ctx.createGain();
          osc.type = 'sine';
          osc.frequency.value = freq;
          gain.gain.setValueAtTime(0.0001, start);
          gain.gain.exponentialRampToValueAtTime(0.4, start+0.02);
          gain.gain.exponentialRampToValueAtTime(0.0001, start+0.28);
          osc.connect(gain); gain.connect(ctx.destination);
          osc.start(start); osc.stop(start+0.3);
        };
        const now = ctx.currentTime;
        for(let round=0; round<3; round++){
          const base = now + round*1.1;
          beepAt(base, 880);
          beepAt(base+0.35, 1046);
        }
      };
      if(ctx.state === 'suspended'){ ctx.resume().then(startBeeps).catch(()=>{}); }
      else{ startBeeps(); }
    }catch(e){ /* audio not available */ }
  }

  function fireReminderAlert(item){
    playAlarmSound();
    if(navigator.vibrate){ navigator.vibrate([300,150,300,150,300,150,300]); }
    showToast('? Reminder: '+(item.title||''));
    if('Notification' in window && Notification.permission==='granted'){
      try{
        new Notification(item.title || 'Reminder', {
          body: item.desc || 'Your reminder time has arrived',
          tag: 'goodplanner-'+(item.id||uid()),
          requireInteraction: true
        });
      }catch(e){}
    }
  }

  function checkReminders(){
    const now = new Date();
    const todayKey = fmtKey(now);
    const nowMinutes = now.getHours()*60 + now.getMinutes();

    // once-off items for today
    const day = storGet('day:'+todayKey);
    let changed = false;
    if(day && day.tasks && day.tasks.length){
      day.tasks.forEach(item=>{
        if(item.type!=='reminder' || !item.time || item.alerted) return;
        const [h,m] = item.time.split(':').map(Number);
        const itemMinutes = h*60+m;
        if(nowMinutes >= itemMinutes && nowMinutes <= itemMinutes+30){
          item.alerted = true;
          changed = true;
          fireReminderAlert(item);
        }
      });
      if(changed){
        storSet('day:'+todayKey, day);
        if(dayCache[todayKey]) dayCache[todayKey] = day;
      }
    }

    // daily-repeating items — tracked per-day via a lightweight alerted map
    const daily = loadDailyItems();
    if(daily.length){
      const d = loadDay(todayKey);
      if(!d.dailyAlerted) d.dailyAlerted = {};
      let dailyChanged = false;
      daily.forEach(item=>{
        if(item.type!=='reminder' || !item.time || d.dailyAlerted[item.id]) return;
        const [h,m] = item.time.split(':').map(Number);
        const itemMinutes = h*60+m;
        if(nowMinutes >= itemMinutes && nowMinutes <= itemMinutes+30){
          d.dailyAlerted[item.id] = true;
          dailyChanged = true;
          fireReminderAlert(item);
        }
      });
      if(dailyChanged){ saveDay(todayKey); }
    }

    if(isSameDay(currentDate, now)) renderToday();
  }

  let wakeLock = null;
  async function requestWakeLock(){
    try{
      if('wakeLock' in navigator && document.visibilityState==='visible'){
        wakeLock = await navigator.wakeLock.request('screen');
      }
    }catch(e){}
  }
  document.addEventListener('visibilitychange', ()=>{
    if(document.visibilityState==='visible' && !Native){
      requestWakeLock();
      checkReminders();
    }
  });

  // ---------- init ----------
  async function init(){
    loadDay(fmtKey(currentDate));
    loadDailyItems();
    loadNotes();
    initTheme();
    initSettings();
    renderToday();
    renderNotes();
    if(Native){
      // Real app build: Android delivers reminders as a true ringing alarm even
      // if this app is closed or the screen is locked, so the in-page JS timer
      // below isn't needed.
      await ensureNotifChannel();
      await resyncNativeReminders();
    } else {
      // Plain browser / preview: best-effort, only fires while this tab is open.
      checkReminders();
      setInterval(checkReminders, 15000);
      requestWakeLock();
    }
    setTimeout(()=>{
      $('#splash').classList.add('hide');
      $('#app').classList.add('show');
      maybeShowOnboarding();
    }, 1300);
  }
  init();
})();
</script>
</body>
</html>
